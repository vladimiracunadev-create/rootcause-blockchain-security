services:
  rootcause-blockchain-security:
    build: .
    ports:
      - "127.0.0.1:8790:8790"
    environment:
      HOST: 0.0.0.0
      PORT: 8790
      DEMO_MODE: "true"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
