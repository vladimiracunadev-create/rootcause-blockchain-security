# Lab 2 — Seguimiento de dirección

```bash
node src/cli.js blockchain address 0x1111111111111111111111111111111111111111 --chain ethereum
```

Revisa balance por activo, transacciones, inflows, outflows, contrapartes,
`first_seen` y `last_seen`. La relación con una dirección no identifica a una
persona ni demuestra control común.
